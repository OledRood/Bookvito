import { createTheme } from '@mui/material/styles';

export const lightTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#6750A4', // Example Material 3 primary color
    },
    secondary: {
      main: '#625B71', // Example Material 3 secondary color
    },
  },
});